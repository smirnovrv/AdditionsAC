
&После("ПриЗаписи")
Процедура АЛФ_ПриЗаписи(Отказ)

	Если ТипЗнч(ВладелецФайла) = Тип("ДокументСсылка.ЗаказПоставщику") Тогда
		ИИТ_ВспомогательныеФункцииСервер.СформироватьСтрокуСчетов(ВладелецФайла);
	КонецЕсли;

КонецПроцедуры
